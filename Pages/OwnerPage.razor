@page "/Duenos"
@inject NavigationManager Nav

<PageTitle>Dueños</PageTitle>

<!-- ================= FILTROS ================= -->

<RadzenStack Orientation="Orientation.Horizontal"
             AlignItems="AlignItems.Center"
             Gap="10"
             Style="padding:10px 20px">

    <RadzenText Text="Dueños" TextStyle="TextStyle.H5"/>

    <RadzenDatePicker @bind-Value="DateSince"
                      Placeholder="Desde"
                      Style="width:160px"/>

    <RadzenDatePicker @bind-Value="DateTo"
                      Placeholder="Hasta"
                      Style="width:160px"/>

    <RadzenTextBox @bind-Value="CityFilter"
                   Placeholder="Ciudad"
                   Style="width:150px"/>

    <RadzenTextBox @bind-Value="PostalCodeFilter"
                   Placeholder="Código Postal"
                   Style="width:120px"/>

    <RadzenButton Icon="filter_alt"
                  Text="Filtrar"
                  ButtonStyle="ButtonStyle.Primary"
                  Click="AplicarFiltros"/>
</RadzenStack>

<!-- ================= BODY ================= -->

<RadzenRow>
    <RadzenColumn Size="12">

        @foreach (var dueno in FilterOwner)
        {
            <RadzenCard Style="
                        margin-bottom:30px;
                        border-radius:16px;
                        box-shadow:0 4px 20px rgba(0,0,0,0.1);
                        overflow:hidden;
                        cursor:pointer;"
                        Click="@(() => Nav.NavigateTo($"/PerfilDueno/{dueno.Id}"))">

                <!-- HEADER DUEÑO -->
                <RadzenStack Orientation="Orientation.Horizontal"
                             AlignItems="AlignItems.Center"
                             JustifyContent="JustifyContent.SpaceBetween"
                             Style="
                                        padding:20px;
                                        background:linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
                                        color:white;">

                    <RadzenStack Orientation="Orientation.Horizontal"
                                 AlignItems="AlignItems.Center"
                                 Gap="15">

                        <RadzenAvatar Image="https://cdn-icons-png.flaticon.com/512/616/616408.png"
                                      Size="70px"
                                      Style="border:3px solid white;"/>

                        <RadzenStack Gap="2">
                            <RadzenLink Path="@($"/PerfilDueno/{dueno.Id}")"
                                        Style="text-decoration:none; color:white;">
                                <RadzenText Text="@dueno.Name"
                                            Style="font-size:22px; font-weight:600;"/>
                            </RadzenLink>

                            <RadzenText Text="@($"Cliente desde {dueno.CreatedAt:yyyy}")"
                                        Style="opacity:0.9; font-size:13px;"/>
                        </RadzenStack>
                    </RadzenStack>

                    <RadzenButton Icon="more_vert"
                                  ButtonStyle="ButtonStyle.Light"
                                  Style="background:transparent; color:white;"
                                  Click="@(() => TogglePets(dueno.Id))"/>
                </RadzenStack>

                <!-- INFO DUEÑO -->
                <RadzenStack Gap="15" Style="padding:20px;">
                    <RadzenRow>
                        <RadzenColumn Size="6">
                            <RadzenText Text="📞 Teléfono" Style="font-weight:600;"/>
                            <RadzenText Text="@dueno.Phone"/>

                            <RadzenText Text="📧 Email" Style="font-weight:600; margin-top:10px;"/>
                            <RadzenText Text="@dueno.Email"/>
                        </RadzenColumn>

                        <RadzenColumn Size="6">
                            <RadzenText Text="🏠 Dirección" Style="font-weight:600;"/>
                            <RadzenText Text="@dueno.Street"/>

                            <RadzenText Text="📝 Notas" Style="font-weight:600; margin-top:10px;"/>
                            <RadzenText Text="@dueno.Coments"/>
                        </RadzenColumn>
                    </RadzenRow>
                </RadzenStack>

                <!-- MASCOTAS -->
                @if (VisiblePets.Contains(dueno.Id))
                {
                    @foreach (var mascota in dueno.Pet)
                    {
                        <RadzenCard Style="
                                    margin:20px;
                                    border-radius:16px;
                                    overflow:hidden;">

                            <RadzenStack Orientation="Orientation.Horizontal"
                                         AlignItems="AlignItems.Center"
                                         Gap="15"
                                         Style="
                                                    padding:20px;
                                                    background:linear-gradient(135deg, #ff9a9e 0%, #fad0c4 100%);
                                                    color:white;">

                                <RadzenAvatar Image="https://cdn-icons-png.flaticon.com/512/194/194279.png"
                                              Size="60px"/>

                                <RadzenStack Gap="2">
                                    <RadzenText Text="@mascota.Name"
                                                Style="font-size:20px; font-weight:600;"/>
                                    <RadzenText Text="@($"Mascota de {dueno.Name}")"
                                                Style="font-size:13px; opacity:0.9;"/>
                                </RadzenStack>
                            </RadzenStack>

                            <RadzenStack Style="padding:20px;">
                                <RadzenRow>
                                    <RadzenColumn Size="6">
                                        <RadzenText Text="🐾 Especie" Style="font-weight:600;"/>
                                        <RadzenText Text="@mascota.Specie"/>

                                        <RadzenText Text="🐶 Raza"
                                                    Style="font-weight:600; margin-top:10px;"/>
                                        <RadzenText Text="@mascota.Breed"/>
                                    </RadzenColumn>

                                    <RadzenColumn Size="6">
                                        <RadzenText Text="🎂 Edad" Style="font-weight:600;"/>
                                        <RadzenText Text="@($"{mascota.Age} años")"/>
                                    </RadzenColumn>
                                </RadzenRow>
                            </RadzenStack>

                        </RadzenCard>
                    }
                }

            </RadzenCard>
        }
    </RadzenColumn>
</RadzenRow>

@code {

    List<Owner> Owners = new();
    List<Owner> FilterOwner = new();

    DateTime? DateSince;
    DateTime? DateTo;

    string CityFilter = "";
    string PostalCodeFilter = "";

    HashSet<string> VisiblePets = new();

    protected override void OnInitialized()
    {
        var dueno1 = new Domain.Entities.Owner
        {
            Id = "1",
            Name = "Juan Pérez",
            Email = "juan@email.com",
            Phone = "123456789",
            Street = "Av. Siempre Viva 742",
            City = "Madrid",
            PostalCode = "28001",
            Coments = "Cliente frecuente",
            CreatedAt = new DateTime(2020, 5, 10),
            Pet = new List<Pet>()
            {
                new Pet
                {
                    Name = "Rocky",
                    Specie = "Perro",
                    Breed = "Labrador",
                    Age = 5
                },
                new Pet
                {
                    Name = "Luna",
                    Specie = "Gato",
                    Breed = "Siames",
                    Age = 3
                }
            }
        };

        var dueno2 = new Domain.Entities.Owner
        {
            Id = "2",
            Name = "María López",
            Email = "maria@email.com",
            Phone = "987654321",
            Street = "Calle Falsa 123",
            City = "Barcelona",
            PostalCode = "08001",
            Coments = "Nueva cliente",
            CreatedAt = new DateTime(2022, 3, 15),
            Pet = new List<Pet>
            {
                new Pet
                {
                    Name = "Michi",
                    Specie = "Gato",
                    Breed = "Criollo",
                    Age = 2
                }
            }
        };

        Owners = new List<Domain.Entities.Owner> { dueno1, dueno2 };

        // Orden inicial
        Owners = Owners.OrderBy(d => d.CreatedAt).ToList();
        FilterOwner = Owners;
    }

    void AplicarFiltros()
    {
        FilterOwner = Owners
            .Where(d =>
                (!DateSince.HasValue || d.CreatedAt >= DateSince) &&
                (!DateTo.HasValue || d.CreatedAt <= DateTo) &&
                (string.IsNullOrEmpty(CityFilter) ||
                 d.City.Contains(CityFilter, StringComparison.OrdinalIgnoreCase)) &&
                (string.IsNullOrEmpty(PostalCodeFilter) ||
                 d.PostalCode.Contains(PostalCodeFilter, StringComparison.OrdinalIgnoreCase))
            )
            .ToList();
    }

    void TogglePets(string duenoId)
    {
        if (VisiblePets.Contains(duenoId))
            VisiblePets.Remove(duenoId);
        else
            VisiblePets.Add(duenoId);
    }
}


